@page
@model PPKProjekt.Pages.CRUDServis.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Servis[0].Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servis[0].Naziv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servis[0].Opis)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servis[0].Cijena)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Servis) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opis)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cijena)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IDServis">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IDServis">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IDServis">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
